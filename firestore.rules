rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // TEMP: allow all reads/writes for hackathon demo
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
